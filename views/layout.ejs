<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Heranças do Sul - Loja de Botas Gaúchas</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" />
    <style>
        html, body {
            height: 100%;
            margin: 0;
            font-family: Arial, sans-serif;
            background-color: #121212;
            color: white;
            display: flex;
            flex-direction: column;
            overflow-y: auto;  /* aqui estava a barra de rolagem ativa */
        }

        .top-bar {
            background-color: #d35400;
            color: black;
            padding: 10px 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .top-bar .logo {
            font-size: 1.8rem;
            font-weight: bold;
        }

        .top-bar .search-bar input {
            padding: 5px 10px;
            border-radius: 5px;
            border: none;
            background-color: white;
            color: black;
        }

        .top-bar .search-bar button {
            background-color: #444;
            color: white;
            border: none;
            padding: 5px 12px;
            border-radius: 5px;
            cursor: pointer;
            margin-left: 5px;
            transition: background-color 0.3s;
        }

        .top-bar .search-bar button:hover {
            background-color: #666;
        }

        .top-bar .icon-buttons {
            display: flex;
            align-items: center;
        }

        .btn-perfil {
            background-color: #2c2c2c;
            color: white;
            border-radius: 50%;
            width: 38px;
            height: 38px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.2rem;
            margin-right: 15px;
            cursor: pointer;
            border: none;
            transition: background-color 0.3s ease;
            text-decoration: none;
        }

        .btn-perfil:hover {
            background-color: #444;
            color: white;
        }

        .btn-cinza-escuro {
            background-color: #2c2c2c;
            color: white;
            border: none;
            padding: 10px 25px;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease;
            text-decoration: none;
            display: inline-block;
        }

        .btn-cinza-escuro:hover {
            background-color: #444;
            color: white;
            text-decoration: none;
        }

        .top-bar .icon-buttons a.btn-cinza-escuro {
            margin-right: 0;
        }

        .nav-bar {
            background-color: #2c2c2c;
            display: flex;
            justify-content: center;
            padding: 10px 0;
            position: relative;
        }

        .nav-bar a,
        .nav-bar button {
            background-color: #2c2c2c;
            color: white;
            border: none;
            margin: 0 10px;
            padding: 10px 20px;
            border-radius: 5px;
            transition: background-color 0.3s;
            cursor: pointer;
            text-decoration: none;
        }

        .nav-bar a:hover,
        .nav-bar button:hover {
            background-color: #444;
        }

        /* Dropdown de Categorias */
        .dropdown-categorias {
            position: relative;
        }

        .categoria-lista {
            display: none;
            position: absolute;
            top: 100%;
            left: 0;
            background-color: #2c2c2c;
            color: white;
            border-radius: 5px;
            margin-top: 5px;
            max-height: 200px;
            overflow-y: auto;
            box-shadow: 0 4px 8px rgba(0,0,0,0.5);
            z-index: 1000;
            width: 180px;
        }

        .categoria-lista a {
            display: block;
            padding: 10px;
            color: white;
            text-decoration: none;
        }

        .categoria-lista a:hover {
            background-color: #444;
        }

        .feature-card {
            border-radius: 15px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.5);
            margin-bottom: 1.5rem;
            padding: 1.5rem;
            background-color: #1e1e1e;
            text-align: center;
            color: white;
        }

        .feature-card h3 {
            font-size: 1.5rem;
            margin-bottom: 1rem;
        }

        .footer {
            background-color: #1c1c1c;
            color: #bbb;
            text-align: center;
            padding: 15px 0;
            position: fixed;
            bottom: 0;
            width: 100%;
            flex-shrink: 0;
            box-shadow: 0 -2px 8px rgba(0, 0, 0, 0.7);
        }

        .content-wrapper {
            flex: 1 0 auto;
            padding-bottom: 60px;
            overflow-y: auto; /* aqui estava ativo */
            max-height: calc(100vh - 120px);
        }

        .btn-cadastrar-form {
            background-color: #2c2c2c !important;
            color: white !important;
            border: none !important;
            padding: 10px 25px !important;
            border-radius: 5px !important;
            cursor: pointer !important;
            transition: background-color 0.3s ease !important;
            display: inline-block !important;
        }

        .btn-cadastrar-form:hover {
            background-color: #444 !important;
            color: white !important;
        }
    </style>
</head>
<body>
    <!-- Topo -->
    <div class="top-bar">
        <div class="logo">Heranças do Sul</div>
        <div class="search-bar">
            <input type="text" placeholder="Buscar..." />
            <button class="btn btn-sm">Buscar</button>
        </div>
        <div class="icon-buttons">
            <a href="/usuarios" class="btn-perfil" title="Perfil"><i class="fas fa-user"></i></a>
            <a href="/produtos/create" class="btn-cinza-escuro btn-sm">Cadastrar Produto</a>
            <i class="fas fa-shopping-cart" title="Carrinho" style="margin-left:15px; cursor:pointer; color:white;"></i>
        </div>
    </div>

    <!-- Navegação -->
    <div class="nav-bar">
        <!-- REMOVIDO: <a href="/produtos" class="btn-cinza-escuro">Todos</a> -->

        <!-- Substituído por botão Categorias -->
        <div class="dropdown-categorias">
            <button class="btn-cinza-escuro" onclick="toggleCategorias()">Categorias</button>
            <div id="categoriaDropdown" class="categoria-lista">
                <% if (typeof categorias !== 'undefined' && categorias.length > 0) { %>
                    <% categorias.forEach(categoria => { %>
                        <a href="/?categoria=<%= categoria.nome %>"><%= categoria.nome %></a>
                    <% }) %>
                <% } else { %>
                    <span style="display:block; padding:10px;">Nenhuma categoria</span>
                <% } %>
            </div>
        </div>

        <a href="#" class="btn-cinza-escuro">Mais Vendidos</a>
        <a href="/produtos/novidades" class="btn-cinza-escuro">Novidades</a>
        <a href="#" class="btn-cinza-escuro">Descontos</a>
    </div>

    <!-- Conteúdo -->
    <div class="container mt-4 content-wrapper">
        <%- body %>
    </div>

    <!-- Rodapé -->
    <footer class="footer">
        <div class="container">
            <span>&copy; 2025 Heranças do Sul</span>
        </div>
    </footer>

    <!-- Scripts -->
    <script>
        function toggleCategorias() {
            const menu = document.getElementById('categoriaDropdown');
            menu.style.display = menu.style.display === 'block' ? 'none' : 'block';
        }

        window.addEventListener('click', function(e) {
            const menu = document.getElementById('categoriaDropdown');
            const botao = document.querySelector('.dropdown-categorias button');
            if (!botao.contains(e.target) && !menu.contains(e.target)) {
                menu.style.display = 'none';
            }
        });
    </script>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
    <script src="https://kit.fontawesome.com/a076d05399.js" crossorigin="anonymous"></script>
</body>
</html>
