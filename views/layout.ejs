<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Heranças do Sul - Loja de Botas Gaúchas</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" />
    <style>
        html, body {
            height: 100%;
            margin: 0;
            font-family: Arial, sans-serif;
            background-color: #121212; /* Preto bem escuro (fundo geral) */
            color: white;
            display: flex;
            flex-direction: column;
        }

        /* Barra Laranja */
        .top-bar {
            background-color: #d35400; /* Laranja */
            color: black; /* Nome preto */
            padding: 10px 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .top-bar .logo {
            font-size: 1.8rem;
            font-weight: bold;
        }

        /* Barra de pesquisa com fundo branco e texto preto */
        .top-bar .search-bar input {
            padding: 5px 10px;
            border-radius: 5px;
            border: none;
            background-color: white;
            color: black;
        }

        .top-bar .search-bar button {
            background-color: #444;
            color: white;
            border: none;
            padding: 5px 12px;
            border-radius: 5px;
            cursor: pointer;
            margin-left: 5px;
            transition: background-color 0.3s;
        }

        .top-bar .search-bar button:hover {
            background-color: #666;
        }

        .top-bar .icon-buttons i {
            font-size: 1.5rem;
            margin-left: 15px;
            cursor: pointer;
            color: white;
        }

        /* Barra de Navegação Cinza Escuro */
        .nav-bar {
            background-color: #2c2c2c; /* Cinza escuro para a barra de navegação */
            display: flex;
            justify-content: center;
            padding: 10px 0;
        }

        .nav-bar button {
            background-color: #2c2c2c;
            color: white;
            border: none;
            margin: 0 10px;
            padding: 10px 20px;
            border-radius: 5px;
            transition: background-color 0.3s;
            cursor: pointer;
        }

        .nav-bar button:hover {
            background-color: #444;
        }

        /* Cards de produtos */
        .feature-card {
            border-radius: 15px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.5);
            margin-bottom: 1.5rem;
            padding: 1.5rem;
            background-color: #1e1e1e; /* Cinza quase preto para destacar os cards */
            text-align: center;
            color: white;
        }

        .feature-card h3 {
            font-size: 1.5rem;
            margin-bottom: 1rem;
        }

        /* Rodapé fixo */
        .footer {
            background-color: #1c1c1c;
            color: #bbb;
            text-align: center;
            padding: 15px 0;
            position: fixed;
            bottom: 0;
            width: 100%;
            flex-shrink: 0;
            box-shadow: 0 -2px 8px rgba(0, 0, 0, 0.7);
        }

        /* Container para que o conteúdo principal não fique por trás do footer */
        .content-wrapper {
            flex: 1 0 auto;
            padding-bottom: 60px; /* Altura do footer para evitar sobreposição */
        }
    </style>
</head>
<body>
    <!-- Barra Laranja Topo -->
    <div class="top-bar">
        <div class="logo">Heranças do Sul</div>
        <div class="search-bar">
            <input type="text" placeholder="Buscar..." />
            <button class="btn btn-sm">Buscar</button>
        </div>
        <div class="icon-buttons">
            <i class="fas fa-shopping-cart" title="Carrinho"></i>
            <i class="fas fa-user-circle" title="Perfil"></i>
        </div>
    </div>

    <!-- Barra de Navegação Cinza Escuro -->
    <div class="nav-bar">
        <button>Todos</button>
        <button>Mais Vendidos</button>
        <button>Novidades</button>
        <button>Descontos</button>
    </div>

    <!-- Conteúdo Dinâmico das Views -->
    <div class="container mt-4 content-wrapper">
        <%- body %>
    </div>

    <!-- Rodapé -->
    <footer class="footer">
        <div class="container">
            <span>&copy; 2025 Heranças do Sul</span>
        </div>
    </footer>

    <!-- Scripts -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
    <!-- Font Awesome para os ícones -->
    <script src="https://kit.fontawesome.com/a076d05399.js" crossorigin="anonymous"></script>
</body>
</html>
